#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

int _printf(const char *format, ...);

int _putchar(char c);

int handle_char(char arg);

int handle_str(char *arg);

int handle_int(int arg);

#endif
